package es.mdef.gestionpedidos.constants;

public class UserEnums {
    public enum Role {
        Admin,
        NotAdmin
    }

    public enum Type {
        Alumno,
        Docente,
        Administracion
    }

    public enum Department {
        EMIES,
        CCESP
    }
}
